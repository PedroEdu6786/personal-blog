import { Text } from '@chakra-ui/layout'

const Date = ({ children }) => {
  return (
    <Text color="portfolio.darkBlue" fontWeight="semibold">
      {children}
    </Text>
  )
}

export default Date
